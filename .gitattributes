# Force LF for all text files (CI runs on Linux)
* text=auto eol=lf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary

# Lock files — keep as-is, no normalization
package-lock.json -text
